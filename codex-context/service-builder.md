# Service Builder

This context is for future simple service prototypes after content workflows are stable.

Start with:

- Local mock data
- Static or file-backed workflows
- Clear boundaries around anything that would become authentication, payments, publishing, or external APIs

Do not add live services until explicitly requested.
